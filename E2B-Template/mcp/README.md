# MCP (in-image)

Only **secret-free local/stdio** MCP servers that every user should have.

## Privileged / multi-user OAuth MCP

Do **not** put Zepto (etc.) API keys here.  
Worker is the MCP client; agent gets toolified calls (see tooling docs).

## TODO

- [ ] Decide any local MCP packages  
- [ ] Register in default Hermes config without secrets  
