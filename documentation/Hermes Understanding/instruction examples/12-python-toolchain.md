# 12 · Python toolchain probe

**Type:** example (from Chitti live dump 2026-07-19 — not fixed in source)

**Probed** via `get_environment_probe_line()` · `tools/env_probe.py`.

Emitted only when the environment is non-default. Example values from Chitti dump.

---

```text
Python toolchain: python3=3.11.15 (no pip module), pip=missing, PEP 668=yes (use venv or uv), uv=installed.
```
